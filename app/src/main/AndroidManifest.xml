<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.course_project_tot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Courseprojecttot">
        <activity
            android:name=".Activity.UserloginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.course_project_tot.Activity.CalendarActivity"/>
        <activity android:name="com.example.course_project_tot.Activity.CreateGoalActivity"/>
        <activity android:name="com.example.course_project_tot.Activity.PomodoroActivity"/>
        <activity android:name="com.example.course_project_tot.Activity.ChartActivity"/>
    </application>
</manifest>